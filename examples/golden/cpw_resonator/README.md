# CPW resonator golden bundle

Status: **awaiting real licensed HFSS evidence**.

Place the reviewed export under `bundle/` and add `expected_results.json` plus
`manual_review.json` according to `../CONTRACT.md`. Do not commit synthetic output
under this name.
