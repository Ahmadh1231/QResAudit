# Licensed HFSS fixtures

Place approved minimal driven and eigenmode `.aedt` fixtures on the self-hosted runner.
Tests in this directory must use `pytest.mark.hfss` and must never upload proprietary
projects or `.aedtresults` content.
